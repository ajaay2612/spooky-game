{
  "enabled": true,
  "name": "Pre-Push Test Suite",
  "description": "Execute the full test suite before allowing code to be pushed. Run all unit tests to verify individual functions and components work correctly, execute integration tests to ensure different parts of the system work together properly, and run game-specific tests like physics simulations and collision detection. Check that test coverage meets the configured threshold (e.g., 80%) and fail the push if coverage drops below the minimum.",
  "version": "1",
  "when": {
    "type": "fileEdited",
    "patterns": [
      "**/*.js",
      "**/*.ts",
      "**/*.jsx",
      "**/*.tsx",
      "**/*.py",
      "**/*.java",
      "**/*.cpp",
      "**/*.c",
      "**/*.cs",
      "**/*.go",
      "**/*.rb",
      "**/*.php"
    ]
  },
  "then": {
    "type": "askAgent",
    "prompt": "A code push is being attempted. Execute the full test suite including:\n1. Run all unit tests to verify individual functions and components\n2. Execute integration tests to ensure system components work together\n3. Run game-specific tests (physics simulations, collision detection, etc.)\n4. Check test coverage meets the configured threshold (minimum 80%)\n5. Report any test failures or coverage drops\n6. Block the push if tests fail or coverage is below threshold\nProvide a comprehensive test report with pass/fail status and coverage metrics."
  }
}